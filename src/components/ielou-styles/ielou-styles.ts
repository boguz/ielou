import { css } from 'lit';
import { ielouCssVariables } from './ielou-css-variables.js';

const ielouSharedStyles = css``;

/**
 * Export variable containing both the css variables and the shared styles
 */
export const ielouStyles = [ielouCssVariables, ielouSharedStyles];
